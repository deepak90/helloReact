import React from 'react';
import './Footer.css';

const Footer= (props) => {
    return (
      <div className="container">
        <p className="text-muted">dpk's site</p>
      </div>
    );
}

export default Footer